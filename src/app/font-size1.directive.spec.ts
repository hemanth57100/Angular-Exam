import { FontSize1Directive } from './font-size1.directive';

describe('FontSize1Directive', () => {
  it('should create an instance', () => {
    const directive = new FontSize1Directive();
    expect(directive).toBeTruthy();
  });
});
